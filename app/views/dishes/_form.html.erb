<%= form_for [current_user, Dish.new] do |f| %>
        <%= f.hidden_field :user_id, value: current_user.id %>
        <%= f.hidden_field :recipe_id, value: recipe.id %>
        <%= f.submit "Add to Dishes", class: "btn btn-md btn-primary btn-muted rounded border border-muted" %>
    <% end %>
